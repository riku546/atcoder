#include <bits/stdc++.h>
#define rep(i, m, n) for (int i = m; i < n; i++)
using ll = long long;
using ull = unsigned long long;
using namespace std;



int func(int temp1 , int temp2, int t, char h, int n)
{
  int cl1 = temp1;
  int cl2 = temp1;
  
  

  
  
}

int main()
{
  int n, q;
  cin >> n >> q;
  int r = 1, l = 2;
  int ans = 0;
  vector<int> temp(n);

  rep(i, 0, q)
  {
    char h;
    int t;
    cin >> h >> t;
    temp[r - 1] = 1;
    temp[l - 1] = 1;
    if (h == 'L')
    {
    }
  }
}