#include <bits/stdc++.h>
#define rep(i, m, n) for (int i = m; i < n; i++)
using ll = long long;
using ull = unsigned long long;
using namespace std;

int binary(vector<int>a , vector<int>b)
{
  int r = 2e10 , l = 0;
  while (r - l > 0)
  {
    int mid = l + (r - l) / 2;
    
  }
  
}

int main()
{
  int n;
  cin >> n;
  vector<int> a(n);
  vector<int> b(n - 1);
  rep(i, 0, n) cin >> a[i];
  rep(i, 0, n - 1) cin >> b[i];

}